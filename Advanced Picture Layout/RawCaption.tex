\documentclass{article}

\usepackage[left=2cm, right=2cm, top=2cm, bottom=2cm]{geometry}
\usepackage[utf8]{inputenc}

\usepackage{caption,subcaption}
\usepackage{floatrow}
\usepackage{graphicx}

%==== Pour mettre une page en format paysage ====

\usepackage{pdflscape}

%==== Pour les entêtes et pieds de page ====

\usepackage{fancyhdr}
\pagestyle{fancy}
\lhead{Entête générée grâce au module \emph{fancyhdr}}
\rhead{page \thepage}
\cfoot{Page mise au format paysage avec le module \emph{pdflscape}}
\renewcommand{\headrulewidth}{0.4pt}
\renewcommand{\footrulewidth}{0.4pt}

\begin{document}

% Pour que la page en format paysage soit "flottante" dans le document et ne brise pas le flow du texte, on peut utiliser le module "afterpage". Voir exemple "floating_landscape_single_page_figure.tex".

\begin{landscape}
% Définir l'espace horizontal entre les sous-figures:
\thisfloatsetup{subfloatrowsep=qquad} 
% Mettre le labellé de la légende à par sur la première ligne, au-dessus du texte:
\captionsetup{labelsep=newline}
\begin{figure}[h]
    \ffigbox
    {} % On indique ici que la légende est vide. Celle-ci sera définie plus loin dans un des deux environnement subfloatrow à l'aire de \RawCaption
    {
    \begin{subfloatrow}[3]
        \ffigbox
        {
         \caption{sub-caption of subfigure a}
         \label{subfig1}
        }
        {\includegraphics[width=\linewidth]{./img/67972605}}
        
        \ffigbox
        {
         \caption{sub-caption of subfigure b}
         \label{subfig2}
        }
        {\includegraphics[width=\linewidth]{./img/67972605}}
        
        \ffigbox
        {
         \caption{sub-caption of subfigure c}
         \label{subfig1}
        }
        {\includegraphics[width=\linewidth]{./img/67972605}}
    \end{subfloatrow}
    
    \vspace{1em}
    
    % Il faut décorer l'environnement subfloat ci-dessous avec * pour indiquer que la légende de la figure se retrouve à l'intérieur de ce subfloatrow.
    
    % Cela est dû au fait que les \caption sont par défaut traités comme des sous-légendes à l'intérieur de l'environnement subfloatrow. En décorant le subfloatrow avec *, on indique alors que \caption ne doit pas être considérée comme une sous-légende. Il est alors nécessaire d'utiliser la commande \subcaption du module "subcaption" pour faire la distinction entre la légende et les sous-légendes dans l'environnement subfloatrow*.
    
    % En temps normal, on pourrait tout simplement utiliser \caption et le subfloatrow automatiquement considérait celles-ci comme des sous-légendes par défaut, tel que pour le premier subfloatrow ci-dessus.
    
    \begin{subfloatrow*}[3]    
        \ffigbox
        {
         \subcaption{sub-caption of subfigure d}
         \label{subfig1}
        }
        {\includegraphics[width=\linewidth]{./img/67972605}}
        
        \ffigbox
        {
         \subcaption{sub-caption of subfigure e}
         \label{subfig1}
        }
        {\includegraphics[width=\linewidth]{./img/67972605}}
        
        \ffigbox
        {
         \RawCaption{ 
         \caption{La commande \emph{$\backslash$RawCaption} permet de libérer le contenu de la légende et de la placer à l'endroit désiré sans perturber la mise en page des éléments à l'intérieur de l'environnement ``float''. Dans cet exemple, la légende de l'assemblage est considérée comme une sixième sous-figure dans la mise en page.}
         \label{fig:param_uncertain}}          
        }
        {} % On indique ici que le contenu du ffigbox est vide. Autrement dit, on a une figure avec juste un caption, pas d'image.
    \end{subfloatrow*}
    }
\end{figure}
\end{landscape}
     
\end{document}